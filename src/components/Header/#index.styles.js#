import styled from "styled-components";
import logo from "@/public/images/fretebras-logo.svg";

export const StyledLogo = styled.img.attrs({
  src: logo,
  height: 32,
})`
  cursor: pointer;
`;

export const StyledLink = styled.a``;

export const StyledRightContent = styled.div`
  display: flex;
  align-items: center;

  @media (max-width: 768px) {
    > .link {
      display: none;
    }
  }

  @media (min-width: 768px) {
    > *:last-child {
      display: none;
    }
  }
`;

export const StyledText = styled.p`
  font-size: 16px;
  font-style: normal;
  font-weight: 500;
  line-height: 26px;
  letter-spacing: 0px;
  text-align: right;
  color: #434343;
`;

export const StyledDropdown = styled.div`
  padding-left: 24px;
  border-left: 2px solid #b8b8b8;
  cursor: pointer;
`;

export const StyledLeftMenu = styled.div`
  position: fixed;
  top: 0;
  bottom: 0;
  left: 0;
  width: 40vw;
  padding: 16px;
  transform: translateX(${({ open }) => (open ? "0" : "-100%")});
  transition: transform 0.5s ease;
  background: white;
  box-shadow: -3px 0 5px 0 #555;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  z-index: 101;

  ${StyledLink}:not(:first-child) {
    margin-top: 24px;
  }
`;

export default styled.div`
  z-index: 100;
  height: 60px;
  background: white;
  display: flex;
  align-items: center;
  padding: 0 34px;
  position: sticky;
  top: 0;
  justify-content: space-between;

  > * {
    z-index: 2;
  }

  &::before {
    position: fixed;
    content: "";
    height: 60px;
    background: white;
    top: 0;
    left: 0;
    right: 0;
    border-bottom: 1px solid #a82827;
    z-index: 1 !important;
  }

  @media (max-width: 768px) {
    padding: 0 16px;
  }
`;
